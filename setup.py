#!/usr/bin/env python
# -*- coding: utf-8 -*-

from distutils.core import setup
import setuptools

setup(
  name = 'pyparadox',
  version = '0.1.1',
  packages = [ 'pyparadox_impl' ],
  py_modules = [ 'pyparadox' ] )

